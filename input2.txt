program  a {
	vars int i, qi, qj;
		bool qb;
	
	func m(int z, y, d; bool l; float p) returns bool {
		return l;
	}

	func q() {
		qb = true;
		qb = false;
		qb = true;

		m(qi + qj * 2, 4 % 2, 5, qb, 2.0);
	}

	func up(bool s) returns float {
		return 2.0 * 5.6;
	}

	main {
		vars float op;
		qb = true;
		qb = m(qi + qj * 2, 4 % 2, 5, qb, 2.0);

		print(qb);

		op = up(true) + up(false);

		print();
	}
}