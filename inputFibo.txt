program a {
	vars int i;

	func fibo(int i) returns int {
		vars int val;

		if (i < 2) {
			val = 1;
		} else {
			val = fibo(i - 1) + (fibo(i - 2));
		}

		return val;
	}

	main {
		vars int i;

		i = 5;

		print(fibo(i));
	}
}