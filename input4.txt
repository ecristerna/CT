program  a {
	func up(bool s, g; float h) returns float {
		return 2 * 5.6;
	}

	main {
		vars float op;
			bool h;
			string let;

		let = "hola";
		h = true;
		op = 5.5;
		print(let, h, op, "%n");
		print(neg(h), neg(up(h, h, op)));
	}
}